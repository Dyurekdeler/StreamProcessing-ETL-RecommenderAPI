"product_view_history" table must be created for Kafka Producer/Consumer apps, script can be found in DBScripts

"bestseller_product" and "products" tables are not needed to be created, they will be automatically created by Spark ETL process app

table data are given as extra, they will be generated by Kafka & Spark when they are ran

TO RUN THE SPRINGBOOT REST API

if using windows os:
mvnw spring-boot:run

if using linux os:
mvn spring-boot:run

note: please make sure to edit applicaiton.properties file according to the local database you are using
